---
paths:
  - Glutt/**/*.swift
  - design-doc/**/*
---

# UI copy

Never use dashes as punctuation in user-facing strings (no em dash, en dash, or spaced hyphen). Use commas, periods, or reword. Hyphenated compounds like `gluten-free` and `plant-based` are fine.
